# 관측중심 재갱신(Observation-Centric Re-Update, ORU)
---
ORU는 손실된 트랙이 새로운 검출과 재연결될 때 누적된 오류를 수정하여 시간 오류 증폭 문제를 해결합니다. 이 과정은 다음을 포함합니다:

**가상 궤적 생성**: 트랙이 손실된 후 재연결될 때, ORU는 마지막으로 본 관측치와 현재 검출 사이에 "가상 궤적(virtual trajectory)"을 생성합니다. 이 궤적은 일정한 속도 가정을 사용하여 두 기준점 사이를 선형으로 보간하여 생성됩니다.

**칼만 필터 재조정**: 이 방법은 칼만 필터 매개변수를 마지막으로 신뢰할 수 있었던 관측치 시점으로 되돌린 다음, 가상 궤적을 따라 반복적으로 재갱신합니다. 이 과정은 다음과 같이 공식화됩니다.

$$  
\text{For } t = t_{\text{last}} \text{ to } t_{\text{current}}: \quad \hat{x}_{t|t} = \text{KF-Update}(\hat{x}_{t|t-1}, z_{\text{virtual},t})  
$$

여기서 $z_{\text{virtual},t}$는 시간 $t$에서의 보간된 가상 관측치를 나타냅니다.

**오류 수정**: 이 재업데이트 프로세스는 오류가 있는 "더미 업데이트"를 관측 기반 예측으로 효과적으로 대체하여 트랙 복구 정확도를 크게 향상시킵니다.
