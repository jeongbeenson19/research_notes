
# **📌 Intrinsic 파라미터와 왜곡**

  

## **1. Intrinsic 파라미터 행렬**

  

내부 파라미터는 카메라 센서 자체의 특성을 나타내며, 일반적으로 다음과 같은 행렬로 표현된다:

  

$$

\mathbf{K} =

\begin{bmatrix}

f_x & \gamma & c_x \

0   & f_y    & c_y \

0   & 0      & 1

\end{bmatrix}

$$

- $f_x, f_y$: 축별 초점 거리 (픽셀 단위)
    
- $(c_x, c_y)$: 주점(principal point), 광학 중심
    
- $\gamma$: skew 계수 (대부분의 카메라는 0)
    

---

## **2. 왜곡 모델**

  

렌즈로 인해 실제 투영은 이상적 핀홀 모델과 차이가 발생한다. OpenCV에서 일반적으로 사용하는 왜곡 모델은 **방사(radial)** + **접선(tangential)** 왜곡을 포함한다.

  

정규화 좌표 $(x,y)$와 반경 $r^2=x^2+y^2$에 대해:

  

### **(1) 방사 왜곡**

  

$$

x’ = x(1+k_1 r^2 + k_2 r^4 + k_3 r^6), \quad

y’ = y(1+k_1 r^2 + k_2 r^4 + k_3 r^6)

$$

- $k_1, k_2, k_3$: 방사 왜곡 계수
    
- 중심에서 멀수록 직선이 곡선으로 왜곡되는 현상
    

  

### **(2) 접선 왜곡**

  

$$

x’’ = x’ + 2p_1 x y + p_2(r^2+2x^2), \quad

y’’ = y’ + p_1(r^2+2y^2) + 2p_2 x y

$$

- $p_1, p_2$: 접선 왜곡 계수
    
- 렌즈와 센서 정렬 불량으로 인해 발생
    

  

최종적으로 보정된 픽셀 좌표는 $(u,v)=K[x’’,y’’,1]^T$ 형태로 계산된다.

---

## **3. 왜곡 추정과 보정 과정**

1. 여러 체커보드 이미지를 통해 코너 대응점 수집
    
2. 초기 투영 모델에서 오차 계산
    
3. 비선형 최적화를 통해 $(f_x,f_y,c_x,c_y,\gamma)$와 $(k_1,k_2,k_3,p_1,p_2)$를 동시에 추정
    
4. 추정된 계수를 사용해 `cv2.undistort` 또는 `cv2.initUndistortRectifyMap`으로 왜곡 보정
    

---

## **4. 품질 지표**

- **RMS 재투영 오차**: 보정된 모델이 얼마나 정확한지 평가하는 기준
    
- 일반적으로 $<1$ 픽셀이면 양호, 0.2~0.5 픽셀이면 매우 우수한 결과
    

---

## **📂 카테고리 제안**

- Computer Vision/Geometry/Calibration
    
- Computer Vision/Geometry/Distortion
    

---
