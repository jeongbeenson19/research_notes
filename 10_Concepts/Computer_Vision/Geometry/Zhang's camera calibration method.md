
# **📌 체커보드 기반 카메라 캘리브레이션 (Zhang’s Method)**

  

## **1. 원리**

  

카메라 보정은 **이미 알고 있는 3D 점(체커보드 코너)**과 **영상에서 추출한 2D 점** 사이의 대응 관계를 이용하여, 카메라의 **내부 파라미터**, **외부 파라미터**, **렌즈 왜곡 계수**를 동시에 추정하는 과정이다.

---

## **2. 카메라 모델**

  

핀홀 카메라 모델을 기반으로 하며, 투영 관계는 다음과 같이 표현된다:

  

$$

s \cdot \begin{bmatrix} u \ v \ 1 \end{bmatrix} = \mathbf{K} \cdot [\mathbf{R} \ | \ \mathbf{t}] \cdot \begin{bmatrix} X \ Y \ Z \ 1 \end{bmatrix}

$$

- $(X,Y,Z)$ : 세계 좌표계 점
    
- $(u,v)$ : 이미지 좌표
    
- $\mathbf{K}$ : 내부 파라미터 행렬
    
- $\mathbf{R}, \mathbf{t}$ : 외부 파라미터 (자세)
    
- $s$ : 스케일 팩터
    

---

## **3. 내부 파라미터**

  

내부 파라미터 행렬은 다음과 같다:

  

$$

\mathbf{K} = \begin{bmatrix}

f_x & \gamma & c_x \

0 & f_y & c_y \

0 & 0 & 1

\end{bmatrix}

$$

- $f_x, f_y$ : 축별 초점 거리 (픽셀 단위)
    
- $(c_x, c_y)$ : 주점(principal point)
    
- $\gamma$ : 비대칭 계수 (skew, 대부분의 카메라는 0)
    

---

## **4. 보정 절차 (Zhang’s Method)**

1. **데이터 수집**
    
    - 체커보드(정사각형 격자)를 다양한 각도와 거리에서 촬영
        
    - 각 이미지에서 코너 좌표 $(u,v)$를 추출
        
    
2. **3D–2D 대응**
    
    - 체커보드 패턴의 실제 3D 좌표 $(X,Y,Z)$는 평면 상에서 정의 가능 ($Z=0$ 가정)
        
    - 각 이미지마다 2D 코너 좌표와 3D 좌표 대응을 형성
        
    
3. **초기 해 추정 (선형)**
    
    - 호모그래피를 통해 카메라 투영 행렬 근사치 계산
        
    - 이를 통해 내부 파라미터 $\mathbf{K}$의 초기값을 구함
        
    
4. **비선형 최적화**
    
    - Levenberg–Marquardt 알고리즘을 사용하여 전체 파라미터 최적화
        
    - 목표 함수: 모든 대응점의 **재투영 오차** 최소화
        
        $$
        
        \epsilon = \sum_{i}| \mathbf{x}_i - \hat{\mathbf{x}}_i(\mathbf{K}, \mathbf{R}, \mathbf{t}, k_1,k_2,p_1,p_2)|^2
        
        $$
        
    
5. **왜곡 계수 추정**
    
    - 방사 왜곡: $k_1, k_2, k_3$
        
    - 접선 왜곡: $p_1, p_2$
        
    - 왜곡 보정 후 정규화된 이미지 좌표를 모델과 일치시킴
        
    

---

## **5. 왜곡 모델**

- 방사 왜곡(radial distortion):
    
    $$
    
    x_d = x(1+k_1r^2+k_2r^4+k_3r^6),\quad
    
    y_d = y(1+k_1r^2+k_2r^4+k_3r^6)
    
    $$
    
- 접선 왜곡(tangential distortion):
    
    $$
    
    x_d = x + 2p_1xy + p_2(r^2+2x^2),\quad
    
    y_d = y + p_1(r^2+2y^2) + 2p_2xy
    
    $$
    
- $r^2 = x^2+y^2$
    

---

## **6. 최종 추정 값**

- 내부 파라미터 $\mathbf{K}$
    
- 각 이미지별 외부 파라미터 $(\mathbf{R}, \mathbf{t})$
    
- 왜곡 계수 $(k_1,k_2,k_3,p_1,p_2)$
    
- RMS 재투영 오차 (품질 지표)
    

---

## **✅ 요약**

- 체커보드 패턴의 **기하학적 규칙성**을 이용해 카메라 파라미터를 추정
    
- 초기 호모그래피 기반 선형 추정 후, 전체 파라미터에 대해 **비선형 최적화** 수행
    
- 최종적으로 내부/외부 파라미터와 왜곡 계수를 동시에 얻음
    

---

## **📂 카테고리 제안**

- Computer Vision/Geometry
    
- 세부 생성 권장:
    
    - Computer Vision/Geometry/Calibration
        
    - Computer Vision/Geometry/Distortion
        
    

---
