좋은 질문입니다. 🎯 F1-score는 머신러닝/정보검색에서 **불균형 데이터**나 **양쪽 지표 균형**이 필요한 상황에서 핵심적으로 쓰입니다. 기계적으로 정리하겠습니다.

---

## **1) 정의**

  

$$F_1 = \frac{2 \cdot \text{Precision} \cdot \text{Recall}}{\text{Precision} + \text{Recall}}$$

- Precision = $\frac{TP}{TP+FP}$
    
- Recall = $\frac{TP}{TP+FN}$
    
- F1은 두 값의 **[[Mean#^6518f6|조화평균(HM)]]**.
    

---

## **2) 주요 쓰임**

  

### **(1)** 

### **불균형 데이터셋**

###  **평가**

- 클래스 간 표본 수 차이가 큰 경우(예: 사기 거래 탐지, 질병 진단).
    
- Accuracy는 대부분의 다수 클래스에 끌려 의미가 없어짐.
    
- F1-score는 “양성 클래스”의 검출 성능을 균형 있게 반영.
    

  

### **(2)** 

### **검색·추천·정보검색(IR)**

###  **분야**

- 검색 시스템(문서 검색, 웹 검색, 추천 알고리즘)에서 결과 품질을 평가.
    
- Precision(정확히 찾음)과 Recall(빠짐없이 찾음) 균형이 중요.
    

  

### **(3)** 

### **자연어처리(NLP)**

- 문서 분류, 개체명 인식(NER), 관계 추출 등.
    
- 특히 불균형 클래스(NER에서 특정 엔티티는 빈도 낮음)에서 성능 지표로 사용.
    

  

### **(4)** 

### **컴퓨터 비전**

- 객체 검출(Object Detection), 세그멘테이션(Segmentation)에서 클래스 단위 F1-score 활용.
    
- 특히 작은 객체나 드문 클래스 평가 시 적합.
    

  

### **(5)** 

### **모델 선택과 튜닝**

- Precision과 Recall 중 하나에 치우치지 않고 모델 성능을 평가.
    
- 하이퍼파라미터 튜닝 시 기준 지표로 활용.
    

---

## **3) 특징**

- **균형 지표**: Precision ↔ Recall 어느 한쪽이라도 낮으면 F1도 낮게 나옴.
    
- **해석**: 단순 평균(AM)이 아닌 조화평균(HM) → 작은 값에 민감.
    
    예: Precision=1.0, Recall=0.1 → F1=0.18 (낮게 평가).
    

---

## **4) 확장**

- **F**_\beta **Score**: Recall을 더 중시할 때 (\beta>1), Precision을 더 중시할 때 (\beta<1).
    
    $$F_\beta = (1+\beta^2)\cdot \frac{PR}{\beta^2 P+R}$$
    
- **Micro / Macro F1**: 다중 클래스에서 집계 방식 차이.
    
    - **Micro**: 전체 TP, FP, FN으로 계산 → 샘플 수 많은 클래스에 민감.
        
    - **Macro**: 클래스별 F1 평균 → 클래스 간 균형 강조.
        
    

---

## **5) 실제 사례**

- **스팸 메일 탐지**: 스팸 놓치면 문제(Recall↓), 정상메일 잘못 스팸처리해도 문제(Precision↓). → F1으로 균형 평가.
    
- **의료 진단 AI**: 희귀병 검출. Recall만 높으면 오탐 많아 환자 불필요 검사 증가, Precision만 높으면 환자 놓침. F1 필수.
    
- **검색엔진**: “정확히 찾되, 놓치지 않아야 한다”는 요구를 수치화.
    

---

👉 요약:

F1-score의 **주요 쓰임**은 **불균형 데이터 상황**에서 **양성 클래스 검출 성능**을 평가하고, **Precision–Recall 균형을 객관화**하는 지표로 활용되는 것입니다.