---
tags:
  - ML
  - learning_rule
  - optimization
aliases:
  - 델타 규칙
  - Widrow-Hoff Rule
---

# Delta Rule (델타 규칙)

**델타 규칙(Delta Rule)**은 지도 학습(Supervised Learning) 환경에서 사용되는 가중치 업데이트 규칙으로, 실제 출력과 목표 출력 간의 **오차(error)를 최소화**하기 위해 경사 하강법(Gradient Descent)을 사용합니다.

주로 단일 계층 신경망, 특히 Adaline(Adaptive Linear Neuron) 모델의 학습에 사용되었으며, 이후 다층 퍼셉트론을 학습시키는 [[Backpropagation|역전파 알고리즘]]의 핵심적인 기반이 되었습니다.

> **요약**: 델타 규칙은 출력값과 목표값의 차이(delta)를 이용하여, 오차를 줄이는 방향으로 신경망의 가중치를 점진적으로 수정하는 학습 방법입니다.

## ⚙️ 작동 원리

델타 규칙은 각 가중치($w_i$)에 대한 업데이트($\Delta w_i$)를 다음과 같이 정의합니다.

`Δw_i = α * (t - y) * x_i`

-   `α` (alpha): 학습률 (Learning Rate), 가중치를 한 번에 얼마나 크게 변경할지 결정합니다.
-   `t` (target): 목표 출력값입니다.
-   `y` (output): 모델의 실제 출력값입니다.
-   `(t - y)`: 오차(error), 즉 '델타'입니다.
-   `x_i`: 해당 가중치($w_i$)에 연결된 입력값입니다.

이 규칙은 오차가 클수록, 그리고 해당 오차에 기여한 입력값이 클수록 가중치를 더 많이 변경하도록 만듭니다.

## ✨ Perceptron과의 차이점

| 구분         | Perceptron의 학습 규칙                                 | Delta Rule (Adaline)                                     |
| ------------ | ------------------------------------------------------ | -------------------------------------------------------- |
| **활성화 함수**  | 계단 함수 (Step Function)                              | 선형 함수 (Linear Function, Identity)                    |
| **오차 계산**    | 클래스 레이블이 맞았는지 틀렸는지 (0 또는 1)           | 실제 출력과 목표 출력 간의 연속적인 값 차이 `(t - y)`    |
| **가중치 업데이트** | 잘못 분류했을 때만 업데이트됩니다.                     | 모든 학습 데이터에 대해 오차에 비례하여 항상 업데이트됩니다. |
| **기반 원리**    | 오분류를 줄이는 방향                                   | 총 오차(예: Mean Squared Error)를 최소화하는 경사 하강법 |

## 🔗 Backpropagation과의 관계

다층 퍼셉트론(MLP)을 위한 **[[Backpropagation|역전파]]** 알고리즘은 델타 규칙을 일반화한 것입니다.

-   **출력층**: 출력층에서의 가중치 업데이트는 델타 규칙과 거의 동일한 형태를 가집니다. 다만, 비선형 활성화 함수(예: [[Softmax|시그모이드 함수]])의 미분값이 추가로 곱해집니다.
-   **은닉층**: 은닉층의 오차는 직접 계산할 수 없으므로, 출력층의 오차('델타')를 가중치를 이용해 역으로 전파시켜 계산합니다. 이 계산된 은닉층의 '델타'를 사용하여 은닉층의 가중치를 업데이트합니다.

결론적으로, **Sigmoid 함수의 미분 가능성**과 **델타 규칙의 오차 기반 가중치 조정 방식**은 역전파 알고리즘의 핵심 구성 요소로 결합되어, 깊은 신경망의 학습을 가능하게 합니다.

## 🔗 관련 개념
-   [[ANN]]
-   [[Perceptron]]
-   [[Backpropagation]]
-   [[Gradient Descent]]