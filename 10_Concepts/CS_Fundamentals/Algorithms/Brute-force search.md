
---

## ✅ 1. **브루트포스 탐색이란?**

### 🔹 **정의**

- **모든 가능한 경우의 수를 전부 탐색**하여 정답을 찾는 알고리즘 기법입니다.
    
- "무식한 힘"이라는 뜻 그대로, **최적화보다는 단순하게 모든 경우를 시도**하는 방법이죠.
    

### 🔹 **장점**

- 구현이 단순하고 직관적입니다.
    
- 작은 입력 크기에서는 빠르고 안전하게 동작합니다.
    

### 🔹 **단점**

- 경우의 수가 커지면 연산량이 기하급수적으로 증가해 비효율적입니다.  
    (시간복잡도가 커짐, 예: O(n²), O(2ⁿ) 등)
    

---

## ✅ 2. **이 문제에서의 브루트포스 적용**

### 🔹 **탐색 대상**

- 우리는 **수열의 길이 k**만 탐색하면 됩니다.
    
- k의 범위가 **L ≤ k ≤ 100**으로 매우 작으므로, 모든 k를 전부 시도해도 충분히 빠릅니다.
    

### 🔹 **탐색 순서**

1. `k = L`부터 100까지 1씩 증가시키며 확인합니다.
    
2. 각 k에 대해 **시작값 a가 존재하는지**를 수학적으로 계산합니다.  
    (즉, 등차수열 공식으로 빠르게 검증 → 여기서도 사실상 수학이 가지치기 역할)
    
3. **가장 먼저 조건을 만족하는 k**가 우리가 찾는 "가장 짧은 수열"이므로, 바로 탐색 종료합니다.
    

### 🔹 **왜 수학 공식을 쓰면서도 브루트포스인가?**

- `k`를 하나씩 전부 시도한다는 점에서 접근 방식은 **브루트포스**입니다.
    
- 다만, **모든 수열을 직접 생성하지 않고 수학 공식으로 검증**한다는 점에서 **수학적 브루트포스**라고 부르는 게 맞습니다.
    

---

## ✅ 3. **시간복잡도**

- k는 최대 100까지 탐색하므로, 최악의 경우 **100번의 반복**만 수행합니다.
    
- 각 반복에서 수행하는 연산은 O(1)입니다.
    
- **총 시간복잡도: O(100) ≈ O(1)** → 매우 효율적입니다.
    

---

## ✅ 4. **브루트포스의 일반적 패턴**

이 문제는 간단한 축에 속하지만, 브루트포스는 보통 아래와 같은 절차를 따릅니다:

1. **모든 후보 생성**: 가능한 모든 k를 순서대로 탐색.
    
2. **조건 검사**: 공식, 시뮬레이션, 비교 등을 통해 유효 여부 확인.
    
3. **정답 선택**: 가장 먼저 조건을 만족하는 답 또는 최적의 답 선택.
    

---

## ✅ 5. **비슷한 브루트포스 문제 예시**

- **연속된 자연수의 합 찾기** (예: 백준 2018번)
    
- **N개의 숫자 중 특정 합을 만드는 조합 찾기** (부분합 문제)
    
- **모든 순열/조합 생성 후 조건 만족 여부 검사** (예: 백준 1182번 "부분수열의 합")
    

---
